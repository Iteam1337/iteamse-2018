{
  "name": "root",
  "private": true,
  "scripts": {
    "api": "lerna run --scope iteam-cms dev",
    "web": "lerna run --scope iteam-se start",
    "test": "lerna run test:once",
    "build:web": "cd packages/iteam-se && NODE_ENV=production npm run build"
  },
  "devDependencies": {
    "husky": "^2.4.1",
    "lerna": "^3.15.0"
  }
}
